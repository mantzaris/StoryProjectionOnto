# Published-prose exploratory amendment

User authorized 2026-09-09: three specified public-domain Gutenberg excerpts,
three query-blind extractions before six contextual extractions, one attempt each.
This is not registered C1/C2, held-out execution, or the full-novel case study.

Same compact eight fields, pinned Qwen3-8B-AWQ revision
`4da05a8edb55c6046cce958586c33b61da07bb79`, nonthinking settings, unconstrained
streaming JSON, 12,288 context and common 2,048 output reserve. Passage-appropriate
actions and explicit reported/promised/questioned/considered content are allowed.
Unsupported numerical validity stays null. No schema expansion or adaptive repair.

Exact source spans and notices: `data/published_prose/`. Questions, lexical
record-only selectors and order: `real_text_poc.py`. Codex-authored reference
alternatives, normalization and manual component-credit rubric are in scorer-only
`real_text_poc.py`, never in transmitted requests. References cover declared scopes,
not every conceivable fact. Unmatched wording is not automatically false; manual
meaning assessment is separate, response-bound, and not independent human review.

History begins at 10,512.544560 allocated seconds. One start, at most nine requests,
at most 450 additional seconds, also below existing exploratory 10,970.219657.
Startup cap 240; live checks 15; each request 40 plus 5 failure-drain; bookkeeping
10; protected shutdown 60 plus 5 guard. The whole deadline takes precedence;
unused authority does not authorize another start or duplicate request. All
previous charges remain. Global 33,660 scheduled / strict actual <36,000 ceilings
and registered inventory are unchanged. Only full-study forecast admission is
excepted for this exploratory batch; no claim that the full study fits.

Both successes and errors are reportable. No perfect-output gate. Relative
narrative time and conditional/modal nuances not captured by this format are
disclosed. Published familiar texts cannot demonstrate unseen-data generalization.
