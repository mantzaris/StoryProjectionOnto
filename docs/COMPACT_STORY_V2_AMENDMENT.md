# Compact-story v2 exploratory amendment

One explicit-null eight-field format and the same unrelated worked example for
both methods. Harbor/Orchard evidence and questions are unchanged; Museum adds
reordered facts, different possession bindings, interval values and belief holders.
Three query-blind calls complete or fail before nine contextual transmissions.
No adaptive tuning or retries. These are not registered C1/C2 constructions.

Source/schema/requests/references/normalization freeze before inference. Null means
unspecified or not attributed here, not false/timeless/no knowledge. Null and absent
nullable values have equal semantic meaning; missing required keys separately fail
format compliance. Known source intervals and beliefs must match actual values.
No qualifications are filled from gold. Selection copies actual records only,
does not count null holder keys as attribution, and never clips source intervals.

Syntax recovery may delete a trailing comma after a value and before ] or } outside
quoted strings. Exact original UTF-8 byte offsets and transformed-text hashes are
recorded. The full transformed document must parse, with duplicate keys and non-JSON
constants rejected. No truncation completion, string changes, default fields or
semantic repair. Original strict failures and scores remain; historical recovery
uses the original scorer and is explicitly derived output, not new model success.

One additional start, at most twelve attempts, at most 700 allocated seconds from
10,263.092129. The effective session maximum is 10,963.092129, within the existing
10,970.219657 exploratory ceiling. Startup 300s, live checks 15s, each generation
45s plus 5s exception drain, validation 10s, protected shutdown 60s plus 5s guard;
whole deadline takes precedence. Same pinned model/sampling, 12,288 context and
common 2,048 output allowance. All historical charges remain. The exploratory
forecast exception is not ordinary-study admission. Global 33,660/<36,000 limits,
registered metrics, C0 and held-out independent review are unchanged.
