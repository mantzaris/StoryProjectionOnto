# Typed-ID small validation session

User-authorized new session `small-typed-semantic-validation-20260908`.
Historical allocation is 5,755.222442 s; one additional service start,
at most three small calls, and at most 1,100 additional allocated seconds.
Maximum global allocation under this authority: 6,855.222442 s. Prior blocks,
failed attempts and unused allowances are neither reset nor reused.
The complete-forecast exception applies only to diagnostics; ordinary execution,
full C1, production-interface activation and held-out execution remain prohibited.
Global scheduled/hard ceilings remain 33,660 / strictly before 36,000 s.

The existing guardian/controller binds source, configuration and reservations.
Startup 360 s, live checks 15 s, each generation 180 s (including client failure
drain), validation 30 s, optional bounded structural clarification preparation
90 s, protected shutdown 60 s, guard 5 s. The whole-session deadline overrides
stage allowances; each generation must fit with validation and shutdown reserved.
No second start. No retry derived from scorer expected answers. A single
structural clarification can quote only observed fact-free contract violations.
An unhealthy transport ends the session. No unbounded sampling or storage walk
is added to the shutdown path.

## Frozen requests and checks

Call 1 reproduces the prepared same-evidence candidate exactly, canonical payload
SHA-256 `fc57ab003859a7bfd7c8075fb2f1b1bec6b102f970238f778c15d392dac83693`.
Template-inclusive capacity: 5,605 + 6,144 / 12,288 tokens. This is a capacity
check, not a generated result. Second example is the already preselected `ev-03`
development passage. Both complete requests, fixtures, rendered templates and
compiled grammars are prepared before allocation. No authored graph is input.
Pinned model, tokenizer, template, sampling, context and whitespace restriction
are unchanged. Typed identifiers cover all destinations including decisions.
Deterministic canonical-ID translation preserves every record and chosen binding;
missing/ambiguous references fail rather than being interpreted by the runtime.

Separate restricted records capture transport, complete JSON, generation schema,
identifier audit, canonical reconstruction, structural diagnostics, endpoint
support, description-support links, temporal support, event connectivity and
construction decisions. Textual entailment still requires readable-output review;
reference links alone do not prove it. Existing scientific validators are retained.

CPU inspection exposed a residual limitation in the historical Phase-1 support
audit: it requires numeric story/validity point 1 for the arrival witness, although
the small evidence provides only a neutral dawn clue and no intrinsic duration.
The diagnostic separately records this inconsistency and unsupported numeric
bounds. It does not amend that oracle, inject a coordinate, or accept an output
simply for satisfying the historical clock assumption. This may prevent full
small scientific acceptance independently of typed-ID performance.

## C0 verification scope

No C0 implementation, denominator, reference or threshold change in this session.
Canonical preserved record: `artifacts/restricted/c0-repaired-extraction-v6/assessment.json`.
Extraction precision 104/122, recall 104/114; all explicit entity, binary relation,
event-role, story-time and validity-time fixture families covered; valid evidence
references 1.00. The four unchanged thresholds pass. Matcher-only record remains
separate (59/154 precision, 59/114 recall; event-role family absent, gate fails).
Contextual projection remains 54/275 precision, 54/91 recall, F1 .295082;
all emitted predictions remain in its denominator. This is not integrated
development acceptance or adequate relevance selection.

## Verification and execution

Initial focused local suite: 56 tests passed in 9.08 s (semantic interface,
typed identifiers, controller/guardian limits, component checks). Existing
transport/resource-shutdown regression checks are reused unchanged.
Remote source/configuration backup is ignored and restricted; a historical remote
summary script that differs from local source is preserved and not overwritten.
The live command is `python scripts/run_capacity_diagnostics.py --semantic --execute`
inside the existing project's persistent named tmux workflow. A CPU-only
`--semantic --prepare-only` run performs packing/decoder/binding checks without
reserving or starting a service. No new PDF or recovery framework is produced.
