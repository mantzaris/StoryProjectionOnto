# Staged exploratory development — pre-generation amendment

## Current bounded C2 continuation (explicit user amendment)

`staged-development-bounded-aux-v3` retains the stages, adapter, evidence and
scientific validators below. It constructs only C2 contexts 1 and 2; C1 and
FixedSelect stay blocked. Historical results are not overwritten. Complete,
mechanically usable graphs are displayed and scored even when scientifically
wrong or unresolved. All assertions stay in the existing scoring denominators;
the separate registered acceptance-gated results remain unchanged.

To bound the observed repeated predicates/references: at most six local types,
eight predicates, four references per list, three aliases, eight decisions,
four omissions/abstentions; labels at most 64 characters, prose 180, contextual
interpretation 320. These are development-only auxiliary ceilings, **not** the
original registered protocol. They may reduce support/coverage; report measured
recall, rare-pivotal preservation and all omissions. No generated records are
trimmed. Required semantic fields and complete source evidence remain supplied.
The same input/output ladder and constraints apply to both contexts. Effective
grammar positive/overflow controls and installed vLLM validation precede launch.

Semantically imperfect but mechanically usable intermediates continue through C.
A blocked stage may receive one justified model repair per context; its dependents
are regenerated if necessary. No CPU semantic repair. This is a versioned
continuation of the same phase, not an accounting reset: actual 8,983.249948 s,
six starts/eleven reservations preserved; maximum 10,316.108081 s, eight starts /
nineteen reservations. Remaining 1,332.858133 s includes protected 60-second
shutdown and 5-second guard. Existing stage caps and global ceilings are unchanged.

## Historical initial staged amendment

Authorized 2026-09-08. This replaces single-response construction only for the
existing easy development demonstration, not production acceptance or held-out
execution. The selected unit remains `dev-unit-01`, context ordinals 1 and 2,
snapshot `070f9abcaefa270873527e3d21509f0f8b366596a7a8cae2f1239fefea34d515`.
The exact snapshot in each immutable selection manifest is controlling; no
reference answers enter requests. No scientific checker or metric changes.

## Field ownership and assembly

| Stage | Model-authored fields | Input |
|---|---|---|
| A | Contextual interpretation; complete local schema (identity, abstraction, types, upper parents, predicate definitions/direction/arity/domain/range/roles/citations); entities (identity, mention assignments, aliases, contextual role/type/abstraction, temporal state, uncertainty, confidence, citations); events (identity, occurrence time, contextual type, reification reason, uncertainty, confidence, citations) | Complete unchanged evidence, upper vocabulary, horizon, budgets; context only for C2 |
| B | Assertion identity, predicate/endpoints or n-ary roles, direction, nested proposition content and any explicit content-identity choice, story/intrinsic-validity/discourse/revelation qualification, holder/attitude/epistemic evidence or direct narration, narrative commitment, confidence, provenance confidence/citations, contextual relevance, unasserted contents | Same evidence plus actual A; context only for C2 |
| C | Node descriptions and exact assertion mappings; assertion `why_matters` and supporting citations; operations with actual inputs/created/removed destinations and supported rationale; omissions and uncertainty/abstentions | Same evidence plus actual A and B; context only for C2 |

The assembler copies A and B fields and adds only the explicitly authored C
annotations at their exact IDs. Missing/duplicate coverage or overwriting a
field fails. The existing nested adapter then mechanically assigns canonical
IDs and administrative execution metadata. It does not choose endpoints,
attribution, time, identity merges, evidence, confidence or descriptions.
Composite request/response hashes bind the actual A/B/C chain; construction
times span A start through C completion; token totals sum those actual calls.
Repairs retain their own records and parent links and are counted separately.

## Frozen interface and checks

`prompts/diagnostics/staged_{A,B,C}.md` define only their owning stage.
Existing node/assertion ceilings remain unchanged; A encodes their aggregate
node ceiling. B binds destination IDs to actual A. C requires one annotation
per actual record and bounds creation targets to the actual inventory.
`uniqueItems` stays in post-validation because pinned vLLM rejects that keyword.
Other invariants remain canonical/structural/scientific checks. Grammar compliance
or an intermediate valid stage is never scientific acceptance.

Same allocation policy for C1 and C2: choose the first template-inclusive input
ceiling that fits from `(7168,5120)`, `(8192,4096)`, `(9216,3072)`, `(10240,2048)`,
`(10752,1536)` input/output pairs. Total context stays 12,288. No truncation of
evidence, dependencies, or essential repair feedback. A repair includes its
actual failed stage as untrusted input; full original responses remain immutable.
Authored ten-node/seven-assertion fixtures verify assembly and capacity, not
evidence-grounding or model success. Actual later requests are packed and checked
by both installed vLLM request validation and XGrammar before transmission.

C1 terminally completes/fails before this experiment's C2 transmissions. C1 has
no query. C2 starts independently from evidence and its revealed context. Only an
eligible actual completed C1 receives a seal and can supply FixedSelect. Earlier
single-response repairs do not retroactively satisfy this new physical ordering.

At most one targeted semantic repair per construction; explicit field ownership
determines its stage. Replacing an earlier stage invalidates all dependents, which
must be generated again. Mixed/uncertain repair ownership is not guessed. An
unresolved scientific assessment does not generate gold-derived feedback or pass.
Complete schema-valid but scientifically failed intermediates may proceed to C
for an inspectable failed graph after their repair is exhausted.

## Accounting and execution

Same phase, not a reset: baseline 6,716.108081 s; staged amendment baseline
8,200.425164 s. Prior four starts/seven reservations persist. New authority is
two starts/fifteen reservations, cumulative six/twenty-two, within remaining
2,115.682917 s and maximum 10,316.108081 s. Startup cap 420 s, live checks 20 s,
generation stage 180 s including exception drain, validation 60 s; protected
shutdown 60 s plus 5 s guard. Whole remaining-phase deadline wins. The existing
controller/guardian, resource monitor, ledger and streaming transport are reused.
The original nine-hour target was not met; 33,660 scheduled and strictly below
36,000 actual remain global limits. The authorized development-only forecast
exception does not admit the unmeasured complete study.

Run with `python -m scripts.run_capacity_diagnostics --staged-development
--prepare-only`, then replace `--prepare-only` with `--execute` in the persistent
project session with a tested commit bound through `STORYPROJECTION_LOCAL_COMMIT`.
Report with `python -m scripts.report_preliminary_development --run <closed-run>`.
Stop the service after work/limits/unsafe state; preserve the pod. No PDF, push,
production inference or held-out execution is authorized by this amendment.

## Bounded repair revision after the three Stage A bases

Starts/reservations 5/10, actual allocation 8,668.935157 s. All three responses
reached 5,120 tokens with incomplete Stage A. C1 remains terminally failed before
the two query-bearing calls. It is not reopened. No B/C graph was assembled.

The context-2 prefix contains 141 duplicate mention-reference occurrences in
nine complete node records. This supplies a specific, source-independent repair
constraint (existing list uniqueness), not a guessed graph. One parent-linked
context-2 Stage A replacement is prepared, followed by B/C only on success and
intact packing. Context 1 has no duplicate-list repair signal and is not repeated.

Revision v2 factors repeated root-budget alternatives in the HUMAN field guide
into a single exact aggregate ceiling; the effective schema and all scientific
fields/validators are unchanged. The same allocation ladder now starts with
`(6144,6144)` when the entire rendered request fits that input ceiling. This is
the existing pinned context, not an enlarged context or a condition-specific
advantage. Parsed-stage repairs still include the previous stage. For a truncated
prefix, the complete original bytes stay in restricted parent lineage; the model
receives complete evidence and every compact confirmed defect, not a CPU-completed
prefix or a cut-down prior answer. This input omission is explicit, not truncation.
The repair can retract unsupported content. No gold, desired binding or missing
semantic field is supplied. One remaining authorized start and at most twelve
remaining reservations are available within 1,647.172924 s; no new allowance.

## Closed outcome

Both authorized additional starts were used. Four staged generations all stopped
at their output caps before completing A; B/C were not transmitted. The targeted
repair removed the observed duplicate reference occurrences but then emitted
eight identical predicate definitions (different IDs) and truncated. This is a
failed staged development configuration on these examples, not universal model
incapability or proof of a particular decoder cause. No further retry series is
initiated. See the generated preliminary report for measured calls, accounting,
actual prefixes and unchanged C0 results. Production and held-out gates remain.
