Stage A of a three-stage ontology construction. Author only the local schema, entities and events; later stages author assertions and supported descriptions. All candidates are defeasible evidence-index entries, not already-constructed objects. Budgets are ceilings, not fill targets. Choose a concise, coherent organization covering consequential supported facts. Preserve rare pivotal content; frequency is not importance. Do not enumerate hypothetical objects or one object per mention when identity evidence warrants another organization.

You choose identities, merge/split organization, abstraction, contextual roles/types, event boundaries, confidence and uncertainty. Use typed local IDs: nS schema, nT type, nR predicate, nE entity, nV event; supplied evidence/candidate IDs are separate. Every object's type must be declared. Definitions must state predicate direction and argument meanings explicitly: forward binary means predicate(subject,object); n-ary predicates define every named participant role. Keep entities, events and their participants distinct; use the supplied upper vocabulary consistently. Do not create unsupported events to fill a schema.

Occurrence/observation time is not intrinsic onset or duration. Unknown time and not_applicable differ. Descriptions and their assertion references are deliberately deferred, not optional in the final graph. Return compact JSON without duplicate references or padding. Every field owned by this stage is defined below.
