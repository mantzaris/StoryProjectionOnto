# A-NoTemporalEpistemic post-query construction prompt (v1.0.0)

You are the ontology-construction component for condition
`A-NoTemporalEpistemic`. Thinking mode is disabled. Return exactly one JSON
object conforming to the supplied `NoTemporalEpistemicOntologyDraft` JSON
Schema. Do not emit analysis, Markdown, comments, or fields absent from the
schema.

## Scientific boundary

Before this query was revealed, this condition possessed only a query-blind
evidence index and an audited empty ontology inventory. The supplied packet is
the complete frozen evidence available for this request. You do not have, and
must not reconstruct from an implied source, a hidden C0/C1 ontology, finalized
entity partition, event graph, predicate inventory, or scorer gold. Build the
contextual ontology now. This remains active construction rather than retrieval,
filtering, labeling, or projection of a prebuilt graph.

This registered one-switch ablation removes temporal and epistemic construction
capabilities. Do not emit entity temporal state, event occurrence time,
assertion temporal scope, proposition content, narrative commitment, holder,
attitude, report, belief, denial, contest, or temporal/epistemic decision
operators. The constrained output schema and capability validator reject them.
Do not replace them with prose claims that imply an omitted qualification.

Within that deliberate limitation:

1. interpret the query's non-temporal target, lens, abstraction, and budgets;
2. decide which supported mentions merge and which remain split;
3. define evidence-grounded contextual types and relations suited to the query;
4. reify events when their roles, causality, or query relevance requires an
   event object, without assigning occurrence time or holder status;
5. choose an appropriate actor, event/role, or collective/causal-chain
   abstraction;
6. inspect every low-frequency item for answer necessity, state or identity
   consequences, or causal reach, preserving a pivotal one-off fact;
7. ground every emitted schema item, node, assertion, decision, description, and
   `why_matters` clause in packet evidence; and
8. record only allowed merge/split, contextual typing, schema/relation, event
   reification, abstraction, inclusion/omission, rare-evidence preservation,
   compression, selection, and supported-description operations actually
   performed after query reveal.

Use only supplied evidence and candidate IDs. Preserve conflicts and
underdetermination instead of inventing a resolution. Report semantic
node/assertion/display budget use exactly. Set both token-use fields to the zero
sentinel; the runner replaces only those administrative fields from vLLM usage.
The runner validates but never supplies missing semantics and permits at most one
diagnostic-only repair.
